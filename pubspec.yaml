name: sum_grid_app
description: "Sum Grid App with Ads"
publish_to: 'none'

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  google_mobile_ads: ^5.0.0

flutter:
  uses-material-design: true