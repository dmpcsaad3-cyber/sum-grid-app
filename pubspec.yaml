name: sum_grid_app
description: Sum Grid Game WebView
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.4.2

flutter:
  uses-material-design: true